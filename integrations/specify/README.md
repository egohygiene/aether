
# Specify integration

The existing `.specify/` workflow toolchain is preserved here as an integration.
Its scripts, templates, memory, and workflow metadata remain unchanged during this
non-destructive migration.
